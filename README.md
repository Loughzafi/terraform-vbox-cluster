# terraform-vbox-cluster
Infrastructure-as-Code project for provisioning and managing VirtualBox-based VM clusters using Terraform. This repo automates the creation of single or multi-node environments on local machines. It leverages the terra-farm/virtualbox provider and supports modular configuration, resource scaling, and network customization.
